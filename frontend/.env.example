# Backend API URL
# For local development, use: http://localhost:8000
# For production, use your GPU server URL (with HTTPS if available)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: API Key for authentication (if you add it to backend)
# NEXT_PUBLIC_API_KEY=your_api_key_here
